spring:
  datasource:
    url: jdbc:postgresql://localhost:5435/starride_db
    username: postgres
    password: biscotte
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      format_sql: true
    properties:
      hibernate:
        format_sql: true